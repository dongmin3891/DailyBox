/**
 * HomePage Component (Pages Layer)
 * 홈페이지의 전체 구성과 레이아웃을 담당
 */

import { HomeLayoutWidget } from '@/widgets/home';

export const HomePage = () => {
    return <HomeLayoutWidget />;
};

export default HomePage;
