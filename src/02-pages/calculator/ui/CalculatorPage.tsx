/**
 * CalculatorPage Component (Pages Layer)
 * 계산기 페이지의 전체 구성과 레이아웃을 담당
 */

import { CalculatorLayoutWidget } from '@/widgets/calculator';

export const CalculatorPage = () => {
    return <CalculatorLayoutWidget />;
};

export default CalculatorPage;
